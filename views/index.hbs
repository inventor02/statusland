<!DOCTYPE html>
<html>
    <head>
        <title>{{ info.hostname }}</title>
        <style>
            body {
                font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Helvetica, Arial, sans-serif;
                background-color: #d8d8d8;
            }
            .container {
                text-align: center;
                align-items: center;
                position: fixed;
                top: 50%;
                left: 50%;
                transform: translate(-50%, -50%);
            }
            .large {
                font-size: 4em;
            }
        </style>
    </head>
    <body>
        <div class="container">
            <div class="large">
                {{ info.hostname }}
            </div>
            <div class="small">
                {{ info.platform }} | {{ info.memory }} RAM | {{ info.load }} CPU | Uptime {{ info.uptime }}
            </div>
        </div>
    </body>
</html>